meta {
  name: Get Me
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{firebaseToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.firebase_uid: eq test-uid-123
}
